spring.application.name=bus

# Server
server.port=${PORT:55555}
server.address=0.0.0.0

# Datasource (usa ENV si existen; si no, cae a tu local)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-soft-truth-aclvpxnf-pooler.sa-east-1.aws.neon.tech/bus?user=neondb_owner&password=npg_LhgG0Ht4zxjc&sslmode=require&channelBinding=require}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:71718902}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hikari (pool pequeño para free tiers)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.connection-timeout=30000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.open-in-view=true
spring.jackson.hibernate.lazy-init=false
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false

